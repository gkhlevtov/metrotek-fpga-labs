# Отчёт по верификации модуля LIFO

**Модуль:** `lifo.sv`

**Параметры симуляции:** `DWIDTH=16`, `AWIDTH=8`, `ALMOST_FULL=2`, `ALMOST_EMPTY=2`, `RANDOM_TESTS=100`

**Seed:** `12345`

---

## Таблица обнаруженных ошибок

| № | Название бага                                                              | Симуляционное время первого воспроизведения | Сообщение в transcript                                              |
| -- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 1  | Отсутствие защиты от Underflow и неверный флаг empty_o | **T = 35 ps**                                                                 | `[SCB] @35: EMPTY mismatch! Got: 0, Exp: 1`                                 |
| 2  | Отсутствие защиты от Overflow                                        | **T = 2655 ps**                                                               | `[SCB] @2655: USEDW mismatch! Got: 257, Exp: 256`                           |
| 3  | Ошибка логики флага `almost_full_o`                                 | **T = 105 ps**                                                                | `[SCB] @105: ALMOST_FULL mismatch! Got: 0, Exp: 1 (usedw=2, ALMOST_FULL=2)` |
| 4  | Ошибка одновременной записи и чтения (`wr=rd=1`)     | **T = 3585 ps**                                                               | `[SCB] @3585: USEDW mismatch! Got: 1, Exp: 4`                               |

---

## Детальное описание ошибок

### Баг №1: Отсутствие защиты от Underflow и неверный флаг `empty_o`

**Описание:**

После синхронного сброса (`srst_i=1`) LIFO находится в пустом состоянии (`usedw_o=0`).
При подаче команды чтения (`rdreq_i=1`) на пустую очередь ожидается, что состояние не изменится.
Фактически DUT декрементирует внутренний указатель без проверки на пустоту, что приводит к целочисленному переполнению счётчика `usedw_o` (wrap-around): значение уходит в `511 = 2^(AWIDTH+1) - 1`.

**Ожидаемое поведение:** `empty_o=1`, `usedw_o=0` - состояние не изменяется.

**Фактическое поведение:** `empty_o=0`, `usedw_o=511`.

**Сообщения в transcript:**

```
[SCB] @35: Read from empty LIFO - underflow protection triggered
** Error: [SCB] @35: EMPTY mismatch!   Got: 0, Exp: 1
           Time: 35 ps  Scope: lifo_pkg.Scoreboard...
** Error: [SCB] @35: USEDW mismatch! Got: 511, Exp: 0
           Time: 35 ps  Scope: lifo_pkg.Scoreboard...
```

**Время первого воспроизведения:** `T = 35 ps`

---

### Баг №2: Отсутствие защиты от Overflow

**Описание:**

После заполнения LIFO до максимальной ёмкости (`usedw_o=256`, `full_o=1`) при подаче дополнительных команд записи (`wrreq_i=1`) ожидается, что состояние не изменится.
Фактически DUT продолжает инкрементировать внутренний указатель без проверки на заполненность, `usedw_o` превышает значение `256`, флаг `full_o` сбрасывается.

**Ожидаемое поведение:** `full_o=1`, `usedw_o=256` - состояние не изменяется.

**Фактическое поведение:** `usedw_o` растёт выше 256.

**Сообщения в transcript:**

```
[SCB] @2655: Write to full LIFO - overflow protection triggered
** Error: [SCB] @2655: USEDW mismatch! Got: 257, Exp: 256
   Time: 2655 ps  Scope: lifo_pkg.Scoreboard.Scoreboard...
```

**Время первого воспроизведения:** `T = 2655 ps`

---

### Баг №3: Ошибка логики флага `almost_full_o`

**Описание:**

Флаг `almost_full_o` должен быть равен `1` при выполнении условия `usedw_o >= ALMOST_FULL_VALUE`.
При `ALMOST_FULL_VALUE=2` флаг обязан подняться начиная с `usedw_o=2`.
Фактически DUT использует инвертированный порог: флаг поднимается при `usedw_o >= 2^AWIDTH - ALMOST_FULL_VALUE = 254`, то есть параметр ошибочно интерпретируется как количество свободных слов, а не занятых.

**Ожидаемое поведение:**`almost_full_o=1` при `usedw_o >= 2`.

**Фактическое поведение:** `almost_full_o=0` при `usedw_o=2..253`, `almost_full_o=1` при `usedw_o >= 254`.

**Сообщения в transcript:**

```
** Error: [SCB] @105: ALMOST_FULL mismatch!  Got: 0, Exp: 1 (usedw=2, ALMOST_FULL=2)
           Time: 105 ps  Scope: lifo_pkg.Scoreboard...
** Error: [SCB] @115: ALMOST_FULL mismatch!  Got: 0, Exp: 1 (usedw=3, ALMOST_FULL=2)
           Time: 115 ps  Scope: lifo_pkg.Scoreboard...
```

**Время первого воспроизведения:** `T = 105 ps`

---

### Баг №4: Ошибка одновременной записи и чтения (`wrreq_i=1, rdreq_i=1`)

**Описание:**

При одновременной подаче `wrreq_i=1` и `rdreq_i=1` (операция WRITE_AND_READ) ожидается, что:

* происходит чтение верхушки стека (`q_o` = текущий top),
* одновременно записывается новое слово (`data_i`),
* размер стека (`usedw_o`) не изменяется (pop + push = нейтральное изменение).

Фактически DUT выполняет **только чтение** (pop без push), игнорируя `wrreq_i`.
Каждое такое событие уменьшает `usedw_o` на 1 лишний раз, что приводит к накопленной расходимости.

**Ожидаемое поведение:** `usedw_o` остаётся неизменным; `q_o` отражает вновь записанные данные.

**Фактическое поведение:** `usedw_o` уменьшается на 1 за каждое событие; новые данные не сохраняются.

**Сообщения в transcript:**

```
** Error: [SCB] @3585: USEDW mismatch! Got: 1, Exp: 4
           Time: 3585 ps  Scope: lifo_pkg.Scoreboard...
** Error: [SCB] @3665: DATA MISMATCH!  Got: ad4c, Exp: ebf2
           Time: 3665 ps  Scope: lifo_pkg.Scoreboard...
```

**Время первого воспроизведения:** `T = 3585 ps`
